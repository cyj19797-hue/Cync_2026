## UI 구현 규칙 (항상 지킬 것)

이 프로젝트는 iOS 기본 시스템 UI를 그대로 쓰지 않고, 자체 디자인 시스템(Design 폴더의 Colors/Typography/Spacing/Radius)을 기반으로 한 커스텀 UI를 지향한다.

다음 시스템 기본 컴포넌트는 스타일이 강제되어 커스터마이징이 어렵기 때문에, 명시적으로 "네이티브 스타일 써도 됨"이라고 요청받지 않는 한 사용을 피한다:
- `.sheet(...)` — 하단 슬라이드 + 드래그 핸들이 자동으로 붙음. 팝업/모달이 필요하면 대신 ZStack + .overlay 기반의 커스텀 모달을 만들 것.
- `.searchable(...)` — 시스템 검색창 스타일이 강제됨. 커스텀 SearchBar 컴포넌트를 사용할 것.
- `.alert(...)`, `.confirmationDialog(...)` — 시스템 알림창 스타일이 강제됨. 확인이 꼭 필요한 파괴적 동작(삭제 등) 외에는 커스텀 팝업을 사용할 것.
- 기본 `TabView` 스타일(iOS 26 리퀴드글래스 포함) — 커스텀 탭바로 대체할 것.
- 기본 `Font`, 기본 `Color` 값 하드코딩 — 항상 DesignSystem의 값을 참조할 것.

새로운 화면이나 컴포넌트를 만들 때:
1. 먼저 DesignSystem 폴더와 기존 Components 폴더를 확인해서 재사용 가능한 스타일/컴포넌트가 있는지 찾는다.
2. 시스템 기본 컴포넌트를 쓰는 게 더 간단하더라도, 위 목록에 해당하면 커스텀으로 구현한다.
3. 정말 시스템 기본 컴포넌트를 쓰는 게 불가피하거나 더 나은 상황이면(예: 접근성, 플랫폼 제약), 임의로 결정하지 말고 이유를 설명하고 사용자에게 먼저 물어본다.
4. 작업 완료 후에는 스크린샷으로 실제로 시스템 기본 UI가 섞여 나오지 않았는지 스스로 확인한다.
